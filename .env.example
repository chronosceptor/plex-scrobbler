# Required - Get from https://trakt.tv/oauth/applications
TRAKT_CLIENT_ID=your_client_id_here
TRAKT_CLIENT_SECRET=your_client_secret_here

# Optional - Server configuration
WEBHOOK_PORT=3000
WEBHOOK_BASE_URL=http://localhost:3000
WEBHOOK_PATH=/webhook

# Optional - User filtering (choose ONE option):

# Option 1: Owner only (recommended)
PLEX_OWNER_ONLY=true

# Option 2: Specific usernames (comment out PLEX_OWNER_ONLY above)
# PLEX_ALLOWED_USERS=username1,username2,username3

# Option 3: Specific user IDs (comment out PLEX_OWNER_ONLY above)  
# PLEX_ALLOWED_USER_IDS=12345,67890,99999

# Optional - Override redirect URI for production
# TRAKT_REDIRECT_URI=https://yourdomain.com/callback